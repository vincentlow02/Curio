#cloud-config
package_update: true
packages:
  - docker.io
  - docker-compose-v2
  - git
runcmd:
  - systemctl enable --now docker
  - usermod -aG docker ubuntu
  - mkdir -p /opt/curio
  - chown ubuntu:ubuntu /opt/curio
final_message: "Curio host prerequisites are ready."
